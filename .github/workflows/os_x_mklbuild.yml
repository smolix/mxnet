name: Apple Silicon smoke

on:
  push:
    branches: [master, onednn-v3-port]
  pull_request:
    branches: [master, onednn-v3-port]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: apple-silicon-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apple-silicon:
    name: Apple Silicon smoke
    uses: ./.github/workflows/macos-arm64-cpu.yml
